# 🔧 V19 API Port Fix - Final Deployment

## 🎯 **ПРОБЛЕМА БЫЛА:**

ApiServer запускался на порту **3333**, а docker-compose пробрасывал **3000**!

**ИСПРАВЛЕНО:** Изменён порт в `src/index.ts` → 3000 ✅

Git commit: **1906ad3**

---

## 🚀 **DEPLOYMENT КОМАНДЫ (КОПИРУЙ В GOOGLE CLOUD):**

### **Команда 1: Pull + Rebuild**
```bash
cd ~/momentum-sniper && git pull && docker-compose up -d --build scanner
```

**Подожди 60 секунд пока собирается!**

---

### **Команда 2: Проверка API**
```bash
curl http://localhost:3000/trade-history
```

**Должен вернуть JSON:**
```json
{
  "trade_history": "FLOW-USD: ENTRY, PnL: OPEN%...",
  "total_trades": 43,
  "win_rate": "52.4",
  "recent_wins": 20,
  "recent_losses": 20
}
```

**Если видишь JSON** ✅ = **API РАБОТАЕТ!**

---

### **Команда 3: Проверка логов**
```bash
docker logs momentum-scanner --tail 30 | grep API
```

**Должно показать:**
```
[API] Server listening on port 3000 (0.0.0.0)
```

---

### **Команда 4: Тест N8N**
```bash
node fire_test_shot.js
```

**Проверь в N8N** → Executions → узел "Read Trade History (V19)" → БЕЗ ошибок! ✅

---

## 🎉 **ПОСЛЕ УСПЕШНОГО ТЕСТА:**

**V19 REFLECTION LEARNING ПОЛНОСТЬЮ АКТИВНО!** 🧠🚀

Бот теперь:
- 🧠 Видит историю 50 трейдов
- 📈 Учится на прошлых успехах и ошибках
- 🐋 Принимает умные решения на основе опыта
