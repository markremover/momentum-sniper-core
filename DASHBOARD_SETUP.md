# Установка Unified Command Center (Меню)

## Что это?

Это единое меню для управления всеми ботами Antigravity с одной команды: `menu`.

## Как установить:

### 1. Скачай скрипт dashboard на сервер

Зайди в терминал Google Cloud и выполни:

```bash
cd ~/momentum-sniper && git pull
```

### 2. Обнови скрипт в Futures Oracle (если нужно)

_(Если хочешь, чтобы меню работало из обеих папок, скопируй скрипт туда тоже):_

```bash
cp ~/momentum-sniper/dashboard.sh ~/futures-oracle/dashboard.sh
chmod +x ~/futures-oracle/dashboard.sh
```

### 3. Создай alias для команды "menu"

Выполни эту команду **один раз**:

```bash
echo 'alias menu="bash ~/momentum-sniper/dashboard.sh"' >> ~/.bashrc && source ~/.bashrc
```

### 4. Проверь работу

Теперь просто набери:

```bash
menu
```

И ты увидишь статус обоих ботов + все команды!

## Что показывает меню:

- ✅ **ACTIVE** (зеленый) - Бот работает
- ❌ **OFFLINE** (красный) - Бот остановлен
- 🟢 **ONLINE & WATCHING** - Бот подключен к рынку и следит
- 🟡 **INITIALIZING** - Бот запускается (подожди 10-30 сек)

## Если бот OFFLINE:

1. Запусти его:
   ```bash
   cd ~/momentum-sniper && docker-compose up -d
   ```

2. Проверь логи на ошибки:
   ```bash
   docker logs momentum-scanner
   ```

3. Если видишь "Exit Code 137" (нехватка памяти) - запусти:
   ```bash
   sudo fallocate -l 2G /swapfile && sudo chmod 600 /swapfile && sudo mkswap /swapfile && sudo swapon /swapfile
   ```
